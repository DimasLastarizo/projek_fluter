name: projek_fluter
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+2

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.2.1
  minimize_flutter_app: ^0.0.2
  sqflite: ^2.4.2+1
  path: ^1.9.1
  intl: ^0.20.2
  uuid: ^4.5.3
 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4
  change_app_package_name: ^1.5.0
 

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/app_icon.png"

flutter:
  uses-material-design: true
